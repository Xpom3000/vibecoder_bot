Короткая заметка для PR:

- UI: в `keyboards/reply.py` текст кнопки "Связаться с человеком" разбит на две строки (emoji сверху, текст снизу) для лучшего отображения на мобильных устройствах.
- Это небольшое визуальное изменение — функционал не затрагивается.

Проверено локально: тесты проходят, изменения запушены в ветку `fix/brief-prefill`.
